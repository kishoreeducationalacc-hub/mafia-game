[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --worker-class geventwebsocket.gunicorn.workers.GeventWebSocketWorker --workers 1 --bind 0.0.0.0:$PORT mafiagame:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
